{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "dockerfile"
    },
    "deploy": {
        "startCommand": "sh -c 'export APP_URL=https://$RAILWAY_PUBLIC_DOMAIN && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT'",
        "healthcheckPath": "/api/health",
        "healthcheckTimeout": 300,
        "restartPolicyType": "on_failure",
        "restartPolicyMaxRetries": 3
    }
}
